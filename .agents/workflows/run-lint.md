---
description: Run ESLint to lint the codebase
---

# Run Lint

When asked to lint the Splitpay codebase, follow these steps:

1. Ensure dependencies are installed:

   ```bash
   npm install
   ```

2. Run the linter:

   ```bash
   npm run lint
   ```

3. Fix any reported issues in the relevant files. You can also use editor integrations to apply some fixes automatically.

